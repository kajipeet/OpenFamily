"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0; // https://nuxt.com/docs/api/configuration/nuxt-config
var _default = exports.default = defineNuxtConfig({
  devtools: { enabled: false },

  modules: [
  '@nuxtjs/tailwindcss',
  '@pinia/nuxt'],


  runtimeConfig: {
    public: {
      apiBase: process.env.NUXT_PUBLIC_API_URL ?? '/api',
      wsBase: process.env.NUXT_PUBLIC_WS_URL ?? '/ws',
      turnServer: process.env.NUXT_PUBLIC_TURN_SERVER ?? '',
      turnSecret: process.env.NUXT_PUBLIC_TURN_SECRET ?? ''
    }
  },

  // SSR for the public landing page, client rendering for the authenticated app.
  routeRules: {
    '/': { ssr: true },
    '/garden': { ssr: false },
    '/app/**': { ssr: false }
  },

  app: {
    head: {
      // Default meta — overridden per page.
      htmlAttrs: { lang: 'en' },
      meta: [
      { charset: 'utf-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' }],

      link: [
      { rel: 'icon', type: 'image/svg+xml', href: '/favicon.svg' }]

    }
  },

  css: ['~/assets/css/main.css'],

  typescript: {
    strict: false,
    shim: false
  }
}); /* v9-8edd21a7b4a616bb */
